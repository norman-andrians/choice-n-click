const typeDf = (el) => {
    if (el.val() != '') {
        console.log('p');
    }
};

$(document).ready(() => {
    const inpText = document.querySelectorAll('.inp-text');

    inpText.forEach((el) => {
        el.children.addEventListener('');
    });
});